import "../styles/sections/controlTips.scss";

export const ControlTips = () => {
    return (
        <div className="controlTips">
            <h4>You can control object by clicking on it and then using arrow keys</h4>
            <h4>Press escape or click on object again to exit arrow control</h4>
        </div>
    );
};
